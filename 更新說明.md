# 電子撲滿 2.2.0

## 安裝

請先同步並下載備份，將網頁與 Mac 都更新至 2.2.0，再使用新功能。舊版無法解讀新增的還原／合併事件。

網頁：解壓縮網頁包，把其中所有檔案及資料夾上傳到 panmstw/Electronic-fluttering 儲存庫根目錄，覆蓋同名檔案。不要只上傳 ZIP 或放在另一層資料夾。等待 GitHub Pages 部署完成後，關閉 iPhone 所有電子撲滿頁面與主畫面捷徑，再連網重新開啟；確認底部顯示 2.2.0。不要清除瀏覽器資料。本次未直接推送或部署 GitHub。

Mac：Apple 晶片、macOS 13 以上。結束舊版，解壓縮更新包，執行「安裝電子撲滿.command」。沿用既有帳本及 Microsoft 登入設定，不必重新建立雲端帳本。此版本未經 Apple 公證，安裝腳本會在本機簽署。原始碼附在包內，詳見使用說明。

入口維持：https://panmstw.github.io/Electronic-fluttering/

## 匯入、取代與合併

在同步設定的「備份與帳本管理」選擇匯入方式，再按「匯入備份」選擇 JSON，或從程式內備份清單選一份，按「套用所選備份」。套用目標是目前正在使用的帳本。

- 取代目前帳本：採用備份的餘額與明細，保留原本帳本及 OneDrive 連接，結果可同步。
- 合併至目前帳本：加入未重複的明細，按新增明細加減餘額。日期、類型、金額、記錄時間皆相同才算重複，也會比較重複筆數。
- 另存為新帳本：建立另一個本機帳本，之後可自行建立雲端帳本。

合併不會直接把備份總餘額相加，因為它可能包含清除歷史後保留的金額，無法確定是否與目前資料重疊。需要完全採用備份餘額時，請選取代。套用前會顯示結果餘額與筆數，確認後先建立「匯入前自動備份」。備份保存失敗便停止操作。

已連接雲端的帳本，套用前必須登入並成功同步；未連接的本機帳本可離線套用。套用後若網路中斷，本機結果仍保存，可稍後再次同步。

還原前請讓其他裝置完成同步並暫停操作。兩端同時還原時，依版本次數及固定操作識別碼選用其中一份，兩端結果一致，不保證採用實際時鐘較晚者。還原時尚未收到的離線新增紀錄會保留。對被取代舊明細的修改不會讓它重新出現。

合併透過固定匯入識別碼避免重試／兩端同時匯入重複計算；它不會撤銷先前對已匯入明細的修改或刪除。要完整回到舊備份內容，請選取代。

## 刪除備份

「備份目前帳本」建立程式內備份；「下載備份」也會留一份在清單。匯入檔案亦會存入清單，取消套用時該副本仍保留。清單可套用、下載或刪除；刪除前需確認。

備份清單只保存在這台裝置，不會同步。刪除清單中的備份不影響帳本，也不會刪除已下載到 Finder／iPhone「檔案」的 JSON；外部下載檔請在原位置刪除。匯入 JSON 後刪除程式內副本，不會刪除原檔。

## 刪除帳本

在「這台裝置保留的帳本」選單選目標，再選擇：

- 僅此裝置：刪除本機帳本，OneDrive 與其他裝置保留，之後可再次選用雲端帳本。
- 刪除所選雲端帳本：需登入原帳號並先成功同步，確認後刪除該 OneDrive 帳本資料夾及此裝置副本。其他裝置的本機副本仍保留，但不能繼續同步至被刪除的位置，需自行處理。雲端列表的帳本需先選用到本機，才能在此刪除。

預設勾選「刪除帳本前保留一份備份」，可取消。刪除目前帳本後切換至另一本；刪除最後一本則建立空白本機帳本。刪除帳本不會自動刪除已有備份，請另外管理。此功能不是安全抹除；系統或歷史版本可能仍保留副本。

## 驗證範圍

Mac 35 項、網頁 29 項自動測試通過，涵蓋還原同步、合併去重、同時操作、備份保存失敗、刪除帳本及模擬雲端刪除的權限／失敗保護。尚未在實體 Mac／iPhone 驗證本次介面與真實 OneDrive 刪除，測試未碰觸使用者雲端資料。
